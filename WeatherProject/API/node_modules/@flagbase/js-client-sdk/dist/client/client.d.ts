import { IApi } from "../api";
import { Config, Identity } from "../context";
import { EventConsumer } from "../events";
export declare type ClientOptions = Config;
export interface IClient extends IApi, EventConsumer {
    destroy: () => void;
}
export default function Client(clientKey: string, identity: Identity, opts?: ClientOptions): IClient;
